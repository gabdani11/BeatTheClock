import { Difficulty, ChallengeStatus, ChallengePhase, SessionResult } from './types';
export declare class BeatTheClockTimer {
    id: string;
    difficulty: Difficulty;
    status: ChallengeStatus;
    durationSeconds: number;
    remainingSeconds: number;
    elapsedSeconds: number;
    pauseCount: number;
    hintsUsed: number;
    currentPhaseIndex: number;
    phases: ChallengePhase[];
    thinkingSeconds: number;
    codingSeconds: number;
    reviewSeconds: number;
    isManualPhase: boolean;
    private intervalId;
    private lastCoachingMessageText;
    private lastResumeTime;
    private accumulatedElapsedMs;
    private listeners;
    onCoachingAlert?: (emoji: string, msg: string) => void;
    constructor();
    subscribe(listener: () => void): () => void;
    private notify;
    start(difficulty: Difficulty, customDurationMinutes?: number): void;
    pause(): void;
    resume(): void;
    stop(): void;
    complete(): SessionResult;
    expire(solved: boolean): SessionResult;
    giveUp(): SessionResult;
    selectPhase(index: number): void;
    incrementHints(): void;
    private startInterval;
    private stopInterval;
    private getAbsoluteElapsedSeconds;
    sync(): void;
    private tick;
    private trackTime;
    private getBreakdown;
    private cleanup;
    getCoachingMessage(): import("./types").CoachingMessage;
    getActivePhase(): ChallengePhase;
}
