{
  "name": "leetcode-timer-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Beat the Clock coding coach monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
